# Blogger Web Frontend - Production Environment Configuration
# This file is loaded only during production builds (vite build)

# API Base URL - Update this with your production API endpoint
# Example: VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:8080
